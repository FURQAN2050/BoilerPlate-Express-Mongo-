#Boilerplate of (express+mongo(mlab))
for run
clone repo.
run npm install. 
change the mlab url from config file.
make the model scheme in models .
make the routes.
and give the path of that route in routes/index.js.

